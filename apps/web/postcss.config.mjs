// Tailwind CSS v4 uses a dedicated PostCSS plugin and CSS-first config.
// See: https://tailwindcss.com/docs/installation
const config = {
  plugins: {
    "@tailwindcss/postcss": {},
  },
};

export default config;
